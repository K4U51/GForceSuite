#pragma once
#include <Arduino.h>

namespace UIHelpers {
  bool detectSwipe() {
    // TODO: Add capacitive/touch swipe detection logic
    return false;
  }

  bool detectTap() {
    // TODO: Detect simple tap for Touch Timer or reset events
    return false;
  }
}
